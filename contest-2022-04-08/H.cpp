#include <bits/stdc++.h>
#include <stdio.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long

using namespace std;

int main() {_
    int c;
    cin >> c;

    while (c--) {
        string polynomial;
        cin.ignore();
        getline(cin, polynomial);
        
        int d, k;
        cin >> d >> k;
        
        
    }

    return 0;
}