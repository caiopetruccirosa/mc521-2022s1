#include <iostream>
#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long

using namespace std;

int main() {_
    int n, m;

    while (cin >> n >> m && n && m) {
        for (int i = 0; i < m; i++) {
            int a, b; ll c;
            cin >> a >> b >> c;
        }
    }

    return 0;
}