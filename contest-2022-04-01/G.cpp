#include <bits/stdc++.h>
#include <stdio.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long

using namespace std;

int main() {_
    array<pair<int, int>, 4> possibilities{ { 1, 0 }, { -1, 0 }, { 0, 1 }, { 0, -1 } };

    int t;
    cin >> t;

    while (t--) {
        int n;
        string s;
        cin >> n >> s;
    }

    return 0;
}