#include <bits/stdc++.h>
#include <stdio.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long

using namespace std;

int main() {_
    string s;
    cin >> s;

    int m;
    cin >> m;

    while (m--) {
        int l, r;
        cin >> l >> r;
    }

    return 0;
}